import React from 'react'
import ActionButton from '../../ActionButton'
export default function RemoveButton() {
  return (
    <ActionButton
      title=''
      afterPressTitle=
      onPress=
    />
  )
}
